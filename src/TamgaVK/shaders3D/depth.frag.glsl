#version 450

// Empty fragment shader for the depth prepass.
// Depth write happens automatically via the fixed-function depth test.
// No color output — this pass has no color attachments.

void main() {
}
